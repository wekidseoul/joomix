import styled from 'styled-components';

const StyledNewComment = styled.div`
  padding: 24px 40px;
  background-color: #f0f0f0;
  border-radius: 8px;
`;

const NewComment = () => {
  return <StyledNewComment>NewComment</StyledNewComment>;
};

export default NewComment;
